import 'package:dasha/ui/shared/navbar.dart';
import 'package:dasha/ui/shared/sidebar.dart';
import 'package:flutter/material.dart';

class DashboardLayout extends StatelessWidget {

  final Widget child;

  const DashboardLayout({
    Key? key, 
    required this.child
  }) : super(key: key);



  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xffEDF1F2),
      
      body: Row(
              children: [
                //TODO: Esto depende si es mas de 700px

                Sidebar(),

                Expanded(
                  child: Column(
                    children: [
                    // Navbar
                    Navbar(),

                    //View
                      Expanded(child: child),
                    ],
                  ),
                )
            ],)
    );
  }
}